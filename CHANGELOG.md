# Changelog

All notable changes to OpenOutreach are documented here.

## Unreleased

### Added

- Hosted `outreachd` control plane on cold-cli engine
- Gmail OAuth + encrypted credential vault + mock provider
- Cloudflare Worker (container proxy, cron tick, tracking pixels, MCP)
- React ops dashboard (Overview → Settings)
- Docs: UPSTREAM, ARCHITECTURE, DEPLOYMENT, GOOGLE_OAUTH, MCP, SECURITY, RELEASING
- Agent context (`AGENTS.md`) and OSS metadata

## v0.1.0

Initial public release (tag cut from `main`).

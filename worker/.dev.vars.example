# Worker secrets (set via `wrangler secret put` or dashboard):
INTERNAL_CONTAINER_TOKEN=
MCP_BEARER_TOKEN=
DATABASE_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
CREDENTIAL_ENCRYPTION_KEY=
PUBLIC_BASE_URL=http://localhost:8787
OPENOUTREACH_WORKSPACE_ID=default
# Required in production: Access application AUD tag (Worker verifies JWT signature + aud)
# CF_ACCESS_AUD=
